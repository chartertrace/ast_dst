<!-- A short description of what this PR changes and why. -->

## Summary

## Checklist
- [ ] `go test ./...`, `go vet ./...`, `gofmt -l .` pass (`golang/`)
- [ ] `npm run typecheck` and `npm test` pass (`typescript/`)
- [ ] If the model changed: `golang/extract/model.go` and `typescript/src/types.ts` updated together, and `sample/model.json` regenerated
- [ ] No invented edges/coverage — gaps render as gaps
